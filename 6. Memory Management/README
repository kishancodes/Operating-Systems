To build, run the following command:

$ make

At any time, run the following command for help:

$ ./oss -h

The program will run with the assigned defaults if you run the following:

$ ./oss


To uninstall after a build, use the following command:

$ make clean


Goal:
The goal of this project is to learn about memory management using virtual memory (paging) in an operating system.
It works on the specified page management algorithm to allocate and deallocate pages of memory.
It specializes in race conditions.
